// Create a function that takes length and width and finds the perimeter of a rectangle.

function peremeter (length, width) {
    return (length+width)*2
}

console.log(peremeter(6,7))
console.log(peremeter(20,10))
console.log(peremeter(2,9))